{% include 'hero' %}

{% schema %}
{
    "name": "Hero",
    "settings": [
        {
            "id": "title",
            "type": "text",
            "label": "Title"
        }
    ],
    "presets": [
        {
            "name": "Hero",
            "category": "hero"
        }
    ]
}
{% endschema %}
