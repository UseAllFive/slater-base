{% include 'hero' %}

{% schema %}
{
    "name": "Hero",
    "settings": [
        {
            "id": "title",
            "type": "text",
            "label": "Title",
            "default": "Find which products are right for you"
        }
    ]
}
{% endschema %}
